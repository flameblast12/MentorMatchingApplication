<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student.mentormatchingapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:label="환영합니다">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:label="@string/login">
        </activity>
        <activity android:name=".SignupActivity" android:label="@string/signup">
    </activity>
        <activity android:name=".SelectScreen" android:label="어떤 사람을 찾으시나요?">
        </activity>
        <activity android:name=".MentorClassListView" android:label="멘토 학과 선택">
        </activity>
        <activity android:name=".MenteeClassListView" android:label="멘티 학과 선택">
        </activity>
    </application>

</manifest>